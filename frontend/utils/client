function useClient() {
  const { user } = useAuth();
  const token = user?.token;

  return React.useCallback(
    (endpoint, config?: {}) => client(endpoint, { ...config, token }),
    [token]
  );
}
